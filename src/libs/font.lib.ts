import { Onest, Lobster } from "next/font/google";

export const fontOnest = Onest({
  variable: "--font-onest",
  subsets: ["latin"],
});

export const fontLobster = Lobster({
  weight: "400",
  variable: "--font-lobster",
  subsets: ["latin"],
});
